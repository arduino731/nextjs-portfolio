/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/nextjs-portfolio", 
    assetPrefix: "/",
    reactStrictMode: true,
}

module.exports = nextConfig
